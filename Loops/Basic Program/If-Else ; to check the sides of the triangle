#include <iostream>
using namespace std;
int main(){
    int a,b,c;
std::cout<<"Enter 1st side=";
std::cin>>a;
std::cout<<"Enter 2nd side=";
std::cin>>b;
std::cout<<"Enter 3rd side=";
std::cin>>c;
if(a+b>c && b+c>a && c+a>b){
    std::cout<<"It is a Valid Triangle";
}
else{
    std::cout<<"It is not a Valid Triangle";
}
return 0;
}
